services:
  - type: web
    name: movia
    env: python
    rootDir: waze/backend
    buildCommand: pip install -r ../../requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT